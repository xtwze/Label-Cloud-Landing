spring:
  datasource:
    url: jdbc:h2:mem:labelcloud;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

labelcloud:
  web:
    allowed-origins: http://localhost:3000
